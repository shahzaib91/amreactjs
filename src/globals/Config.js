const Config = () =>
{
    // global configureable params
    const baseUrl               =   "http://localhost:8080/";
    const appName               =   "AwesomeMotive Blog";

    return { baseUrl, appName, }
}
export default Config;